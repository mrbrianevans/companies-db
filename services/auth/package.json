{
  "name": "auth",
  "description": "Authentication and rate limiting",
  "type": "module",
  "scripts": {
    "start": "node index | pino-pretty -c -t",
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "watch": "tsc -b -w"
  },
  "version": "1.0.0",
  "dependencies": {
    "@fastify/redis": "^6.0.0",
    "fastify": "^4.5.3",
    "ioredis": "^5.2.3",
    "pino-loki": "^2.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.7.14",
    "json-schema-to-ts": "^2.5.5",
    "pino-pretty": "^9.1.0",
    "typescript": "^4.8.2"
  }
}
